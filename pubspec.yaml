name: zero_injector
description: ZeroInjector - Android injector bypass internet tanpa root
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  device_info_plus: ^9.1.0
  flutter_user_agentx: ^1.0.2
  process_run: ^0.12.5+2
  flutter_local_notifications: ^16.1.0
  background_fetch: ^1.2.1
  provider: ^6.0.5
  webview_flutter: ^4.4.2
  shared_preferences: ^2.2.2
  permission_handler: ^11.0.1
  connectivity_plus: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/stunnel_template.conf
    - assets/payload_templates.json